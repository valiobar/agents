Put global knowledge-base files for the inventory agent in this folder.

Supported formats:

- `.pdf`
- `.txt`
- `.md`

When `PRELOAD_INVENTORY_DOCS=true`, the Knowledge Base service loads these files on startup into the shared `global_inventory` ChromaDB collection.
