Put global knowledge-base files for the accountant agent in this folder.

Supported formats:

- `.pdf`
- `.txt`
- `.md`

When `PRELOAD_TAX_DOCS=true`, the Knowledge Base service loads these files on startup into the shared `global_tax` ChromaDB collection.
